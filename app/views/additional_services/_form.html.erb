<%= form_with(model: additional_service) do |form| %>
  <% if additional_service.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(additional_service.errors.count, "error") %> prohibited this additional_service from being saved:</h2>

      <ul>
        <% additional_service.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :service_name, style: "display: block" %>
    <%= form.text_field :service_name %>
  </div>

  <div>
    <%= form.label :service_value, style: "display: block" %>
    <%= form.text_field :service_value %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
